    
    StimDurVect = [200 400 800];
    
    for i = 1:3
        params.StimDuration = StimDurVect(i);
        params.StimDir = 1;
    
    [cNoiseCoef_ctrl,cNoiseCoef_ctrlIndex] = NoiseCorrelations(dirlist_Naive,params);